---
include:
  - board: thinkpad_wireless
    # studio-rpc-usb-uart: ZMK Studio 通过 USB 虚拟串口连接。
    #
    # NOTE: zmk-usb-logging 已移除 —— Studio RPC 与 USB 日志共用同一个
    # CDC ACM 口，两者会互相交错误码文本与 Studio 二进制 RPC 帧
    # （review 2026-08-13 item 2.12）。需要串口日志调试时，在本地构建
    # 追加 snippet: zmk-usb-logging（并临时去掉 studio-rpc-usb-uart）。
    snippet: studio-rpc-usb-uart
