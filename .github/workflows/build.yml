name: Build

on:
  push:
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config_path: "Code/zmk-config"
      build_matrix_path: "Code/zmk-config/build.yaml"